# TODO - Custom Trip feature

- [x] Locate frontend file `services/customTrips.js`.

- [x] Create `Models/CustomTripModel.js` (schema per requirements).
- [x] Create `Controllers/CustomTripController.js` (POST + GET user + GET by id).
- [x] Create `Routers/CustomTripRoutes.js`.
- [x] Register route in `Server.js` as `app.use('/api/custom-trips', customTripRoutes);`.

- [ ] Update frontend `services/customTrips.js` to call the 3 endpoints.
- [ ] Smoke test the new endpoints (run server + verify Mongo storage).



